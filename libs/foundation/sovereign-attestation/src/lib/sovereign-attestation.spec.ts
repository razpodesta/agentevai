import { sovereignAttestation } from './sovereign-attestation';

describe('sovereignAttestation', () => {
  it('should work', () => {
    expect(sovereignAttestation()).toEqual('sovereign-attestation');
  });
});
