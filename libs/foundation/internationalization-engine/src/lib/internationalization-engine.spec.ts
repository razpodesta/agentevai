import { internationalizationEngine } from './internationalization-engine';

describe('internationalizationEngine', () => {
  it('should work', () => {
    expect(internationalizationEngine()).toEqual('internationalization-engine');
  });
});
