import { jurisdictionResolver } from './jurisdiction-resolver';

describe('jurisdictionResolver', () => {
  it('should work', () => {
    expect(jurisdictionResolver()).toEqual('jurisdiction-resolver');
  });
});
