import { behavioralSentinel } from './behavioral-sentinel';

describe('behavioralSentinel', () => {
  it('should work', () => {
    expect(behavioralSentinel()).toEqual('behavioral-sentinel');
  });
});
