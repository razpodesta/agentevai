import { gamificationEngine } from './gamification-engine';

describe('gamificationEngine', () => {
  it('should work', () => {
    expect(gamificationEngine()).toEqual('gamification-engine');
  });
});
